# Installed by `npm install` on the Codemagic build lane — never committed.
node_modules/
package-lock.json

# Generated by `npx cap add ios` / `npx cap sync ios` on the build lane.
ios/

.DS_Store

# Screenshot capture scratch (regenerated by store/capture-screenshots.mjs)
store/.scenes/
